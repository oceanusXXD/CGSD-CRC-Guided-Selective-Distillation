# LROBench End-to-End Comparison Status

Included artifacts:
- `table0_zero_shot_crc_baseline.csv`: pooled zero-shot + CRC baseline, including per-query rows.
- `table1_zero_shot_vs_dbds60.csv`: zero-shot + CRC vs DBDS-60 SFT + CRC.

Scope: LROBench pooled ordered run only. FEVER and external 4B / GPT-5 comparison baselines are not included.

This is a partial end-to-end comparison for the current LROBench run, not the full paper-level comparison table.
