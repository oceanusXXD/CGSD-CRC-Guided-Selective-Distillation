# LROBench CRC Guarantee Status

Included artifact:
- `table3_budget_curve.csv`: contains `risk_accept_wrong`, `err_accept`, `crc_grid_feasible`, and `crc_risk_bound` for the LROBench budget scan.

Scope: LROBench pooled ordered run only.

The original plan asks for 20 repeated random splits and alpha sweeps. Those full CRC guarantee experiments have not been run for LROBench here.
