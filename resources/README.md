Resources

This directory is reserved for small public resources that belong with the code package.

Generated datasets, experiment outputs, result bundles, checkpoints, logs, and paper-specific tables are intentionally kept out of the cleaned tree. Put those artifacts in an external run directory or another ignored location instead of cross-linking them from the reusable code.
